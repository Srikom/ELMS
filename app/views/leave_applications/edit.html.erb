<script type="text/javascript">
        $("#leave_application_start_date").datepicker({

                dateFormat : 'yy-mm-dd',
                readOnly : 'true'

        });
        $("#leave_application_start_date").datepicker("setDate","<%= (@sd.blank? ? @date : @sd) %>");
</script>
<h1>Edit Leave Application#<%= @leaveApplication.id %></h1>
<%= form_for (@leaveApplication) do |f| %>
<center>
<div id="reportFilter2">
<fieldset>
	<legend><b>New Application</b></legend>
<% if @leaveApplication.errors.any? %>
	<div id="formError">
		<h2>Errors :</h2>
		<ul>
			<% @leaveApplication.errors.full_messages.each do |message| %>
				<li><%= message %></li>
			<% end %>
		</ul>
	</div>
<% end %>
<div class="form_text_box">
        <label><b>Start Date:</b></label><%= f.text_field :start_date,disabled: true,readOnly: true %><br/>
        <label><b>End Date:</b></label><%= f.text_field :end_date, :value => (@ed.blank? ? '' : @ed) %>
</div>
	
 <h1>Type of leave</h1>
 <div>
 <%= f.select :leave_id , options_for_select([['Select Leave Type',""],['Normal Leave',1],['Sick Leave',2],['Emergency Leave',3]]) %>
</div>
 <h1>Reason</h1>   
 <div class="form_text_box">
      <%= f.text_area :reason %>
 </div>
 <%= f.hidden_field :start_date , value: (@sd.blank? ? @date : @sd) %>
 <%= f.submit 'Save As Draft', name: "save", class: "save"%>
 <%= f.submit 'Submit Application', name: "submit", class: "submit"%>
</fieldset>
</div>
</center>
<% end %>
<%= link_to 'Back', :back %>




